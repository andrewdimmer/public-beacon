type Beach {
  id: ID!
  countryId: ID!
  postalCodeId: ID!
  name: String!
  address1: String!
  address2: String
  city: String
  region: String
}

input CreateBeachInput {
  countryId: ID!
  postalCodeId: ID!
  name: String!
  address1: String!
  address2: String
  city: String
  region: String
}
